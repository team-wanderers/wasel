# wasel

Next.js + PostgreSQL + Drizzle.

```bash
cp .env.example .env
docker compose up -d
npm install
npm run db:push
npm run dev
```

```bash
npm run lint
npm run typecheck
npm run db:generate
npm run db:migrate
npm run db:studio
```
