services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: wasel
      POSTGRES_PASSWORD: wasel
      POSTGRES_DB: wasel
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
